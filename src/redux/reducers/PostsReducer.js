
const INITIAL_STATE = [
    { postTitle: "First Post" },
    { postTitle: "Second Post" }
]


export const PostReducer = (state=INITIAL_STATE, action) => {

    // custom logic based upon action // you can write it here

    return state;
}