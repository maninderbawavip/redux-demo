
const INITIAL_STATE = [
    {title: "to buy griceries", description: "need to buy kaddu tinda baingan"}
]

export const NotesReducer = (state=INITIAL_STATE, action) => {

    // write any custom logic based upon received action
    
    return state;
}